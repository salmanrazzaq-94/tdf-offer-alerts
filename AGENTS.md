# AGENTS.md

Be optimistic and positive.

## Project

This is a private TypeScript project for TDF offer alerts, Telegram messaging, and Cloudflare Worker deployment.

## Development

- Use `npm run check` for TypeScript verification.
- Use `npm test` for the full local test command.
- Use `npm run build` when generated `dist/` output is needed.
- Do not commit secrets or local environment values from `.env`.

## Git Identity

For commits made from this clone, use:

- Name: Salman Razzaq
- Email: salmanrazzaq94@gmail.com
